# Dependencies & build output
node_modules
dist
coverage

# Logs & OS cruft
*.log
.DS_Store

# Per-session hook state (scorecard mirror, trajectory, baseline, last-surfaced
# dedup marker) — must never pollute the audited diff (Story 1.1, AC5).
.blastcheck/

# Non-functional artifacts (kept local for now)
_bmad/
_bmad-output/
.claude/
docs/
agentcheck-v1-spec.md
agentcheck-v1-spec-en.md
